- Include check-nan.
- Include an U0 to save the t_{n-1} time-step.
- Include a module to print U0 when check-nan == true.

- Define rho = 0, pre = 1, vx1 = 2, vx2 = 3, vx3 = 4
  in order to use them as labels for the U(n) vector.
