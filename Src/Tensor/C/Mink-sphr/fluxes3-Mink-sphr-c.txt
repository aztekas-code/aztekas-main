pow(W, 2)*h*rho*w/(pow(r, 2)*pow(sin(theta), 2))
pow(W, 2)*h*rho*u*w/(pow(r, 2)*pow(sin(theta), 2))
pow(W, 2)*h*rho*v*w/(pow(r, 2)*pow(sin(theta), 2))
pow(W, 2)*h*rho*pow(w, 2)/(pow(r, 2)*pow(sin(theta), 2)) + p
0
0
0
W*rho*w/(pow(r, 2)*pow(sin(theta), 2))
